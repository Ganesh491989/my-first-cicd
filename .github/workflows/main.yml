# .github/workflows/ci-demo.yml
name: CI Demo

on:
  push:
    branches: [main]

jobs:
  hello-world:
    runs-on: ubuntu-latest
    steps:
      - name: Say Hello
        run: echo "Hello from GitHub Actions!"
